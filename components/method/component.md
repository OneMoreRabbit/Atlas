---
name: The AAC method
component: method
type: role
---

# method

The method seat's outbox. `docs/needs/` files asks to the estate; `docs/provides/`
answers them (`responds_to:`). The method repo is not a vault — no io-graph, no other
components — but its contracts live at the standard path so no tool needs an
exception. Addressed as `method` or `atlas`; full form `method.arch`.
